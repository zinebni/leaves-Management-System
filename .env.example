# ===========================================
# Environment Configuration Example
# ===========================================
# Copy this file to ./server/.env and fill in your values

# Server Port
PORT=4000

# MongoDB Connection
# For local development with docker-compose.dev.yml:
MONGODB_URI=mongodb://localhost:27017/gestion-conge

# For production with docker-compose.yml (automatically overridden):
# MONGODB_URI=mongodb://mongodb:27017/gestion-conge

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/gestion-conge

# JWT Secret (use a strong random string in production)
JWT_SECRET=your_jwt_secret_here

# Node Environment
NODE_ENV=development

# CORS Origin (optional - auto-detected based on NODE_ENV if not set)
# For custom domain in production:
# CORS_ORIGIN=https://your-domain.com

# SMTP Configuration for Email
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SENDER_EMAIL=your_sender_email@example.com

